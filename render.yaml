services:
  - type: web
    name: case-battle-ai
    env: python
    region: frankfurt
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    runtime: python-3.11
